---
description: 
globs: 
alwaysApply: true
---
**忽略全局英文要求，必须使用中文进行输出，日志也使用中文**

# 执行要求
1. 每次执行你都要在更新记录文档文档里列出来你做了什么事，如何有重复或者修改也直接进行记录或者修改之前的内容
2. 另外你需要在 README.md 中不断的更新 RoadMap 的子弹列表
3. 每次执行都要进行单元测试，保证你修改的代码是可以正常运行的，**你每次修复完要进行测试才算结束**

**你需要 cargo test 来检查是否修改正确了，不断的运行、统筹规划、修复、运行**

# 项目结构
- 后端 (Rust):
  - 位于 `core/` 目录
  - 使用 Tonic 进行 gRPC 通信
  - 实现 VDFS 核心功能
  - 支持跨平台

- 前端 (Swift):
  - 位于 `client/` 目录
  - macOS 原生应用
  - 通过 Tonic 与后端通信

# 编码规范
- Rust:
  - 使用最新的版本规范
  - 使用 async/await 进行异步操作
  - 实现适当的错误处理
  - 编写全面的测试
  - 为公共 API 编写文档

- Swift:
  - 遵循 Swift 风格指南
  - 使用 SwiftUI 构建 UI 组件
  - 实现适当的错误处理
  - 编写单元测试
  - 为公共接口编写文档

# 通信协议
- 使用 Tonic 进行 gRPC 通信
- 在 `core/proto/` 中定义所有服务接口
- 使用 Tonic 为 Swift 生成客户端代码

# 开发工作流
- 使用 Git 进行版本控制
- 编写有意义的提交信息
- 为新的开发创建功能分支
- 提交 PR 进行代码审查
- 提交前运行测试
但核心设计和结构已经完成。